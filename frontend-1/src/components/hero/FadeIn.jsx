// Re-exported so existing imports of `components/hero/FadeIn` keep working.
// The actual implementation lives in `components/ui/FadeIn` alongside the
// other reusable primitives.
export { default } from "../ui/FadeIn";
